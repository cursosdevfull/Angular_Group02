import { ErrorFormDirective } from './error-form.directive';

describe('ErrorFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorFormDirective();
    expect(directive).toBeTruthy();
  });
});
